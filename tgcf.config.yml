admins: [chaula,Skylark21] 
# when tgcf is run in live mode, the admins can run commands to change the configuration

forwards:
  - source: unfolded
    dest: [AirdropDailyAlertsLK,https://t.me/AirdropDailyAlertsLK]
    # use username or link of the entity


show_forwarded_from: false

plugins:
  filter:
    text:
      blacklist: ["nope"]
  replace:
    text:
      god: devil
      tokyo: delhi
      
